name: 03 - Workflow Runners
on: push

jobs:
  Ubuntu-echo:
    runs-on: Ubuntu-latest
    steps:
       - name: Show OS
         run: |
             echo"this is running on Ubuntu runner"
             echo"Runner OS: $RUNNERS_OS"     
